<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chhabi.finalassignment">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.FinalAssignment">
        <activity android:name=".activities.ui.UI.LoginActivity"></activity>
        <!-- <activity android:name=".activities.ui.UI.CustomerviewActivity"></activity> -->
        <!-- <activity android:name=".activities.ui.UI.DashboardActivity" /> -->
        <activity android:name=".activities.ui.UI.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ui.UI.SignupActivity" />
        <activity android:name=".activities.ui.UI.MainActivity" />
    </application>

</manifest>