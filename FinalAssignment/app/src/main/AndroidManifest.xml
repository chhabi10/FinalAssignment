<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.chhabi.finalassignment">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinalAssignment"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.ui.UI.MainActivity"></activity>
        <activity android:name=".activities.ui.UI.DashboardActivity" />
        <activity android:name=".activities.ui.UI.AddUserActivity" />
        <activity android:name=".activities.ui.UI.ViewuserActivity" />
        <activity android:name=".activities.ui.UI.LoginActivity" />
        <activity android:name=".activities.ui.UI.SplashscreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.ui.UI.SignupActivity" />
    </application>

</manifest>